# V3 — Deferred, Gated

## REN-168: genuine basket co-occurrence signal — EXPLICITLY DEFERRED

Not scheduled. Not designed in detail (per orchestrator instruction and the issue's own "do not build speculatively" tracking status). See `VERSION_TRIGGERS.md` for what would need to be true before this moves out of V3 into an actual scoping pass.

Directional shape only (per `05-algorithms/TARGET_ALGORITHM.md`'s V2/V3 note): a co-purchase data pipeline over `orderItems` history, feeding a precomputed pair-affinity lookup — distinct in kind from the existing single-item similarity call, likely requiring new batch-processing infrastructure not present today.

## Nothing else is currently in V3

No other confirmed or speculative item in this package's scope extends beyond V1/V2 — this Epic's known backlog is fully accounted for by V1 (4 confirmed fixes) and V2 (1 conditional verification outcome).
