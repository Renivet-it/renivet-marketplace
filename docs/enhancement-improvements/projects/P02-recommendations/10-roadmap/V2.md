# V2 — Conditional on Verification

## REN-165: post-purchase recommendation surface — CONDITIONAL

V2 scope for this item exists **only if** `09-validation/EXPERIMENT_STRATEGY.md`'s Phase 1 verification produces a GO or a "promising enough to pilot" verdict. This file does not pre-authorize the build.

If verification supports proceeding, directional shape (per `05-algorithms/TARGET_ALGORITHM.md`'s Placement D note, not a committed design):
- Reuse Placement C's Postgres-based order-history scorer as the starting algorithm (freshest signal is the order just placed).
- Add impression/click instrumentation alongside the build, since it doesn't exist today (closes the `08-reliability/OBSERVABILITY.md` gap for this surface at minimum).
- Ship as a pilot/flagged rollout first (per `EXPERIMENT_STRATEGY.md` Phase 2), not a full rollout, given PROBABLE-not-confirmed origin.

## Also candidate for V2 (not gated on REN-165, but not in V1 either)

- Recommendation-specific impression/click event schema more broadly (benefits measuring all three existing placements' quality, not just a hypothetical post-purchase one) — named in `06-data/DATA_REQUIREMENTS.md` and `09-validation/SUCCESS_METRICS.md` as a real gap, but not one of the six tracked Linear issues, so not committed here. **DECISION REQUIRED** whether this becomes its own tracked issue.
