# V1 — P05 Customer Journey & UX

V1 is remediation of what's broken, not new capability. Two highest-leverage items, plus the rest of the Epic's backlog.

## V1.1 — REN-144: transaction boundary + reconciliation (highest priority, GO)
- `db.transaction()` around order/orderItems creation (FR-1.2).
- Fix the success criterion so partial completion is never reported as full success.
- Reconciliation record (FR-1.3) and detection mechanism (FR-1.4).
- Resolve webhook-vs-client-path relationship (FR-1.5) — may land as a fast-follow if it requires more design time than the transaction fix itself.
- Interim, ship-immediately mitigation (does not need to wait for the rest): add the partial-order-creation alert described in `08-reliability/OBSERVABILITY.md`.

## V1.2 — REN-95: guest checkout (GO WITH CONDITIONS)
Blocked on the 6 decisions in `07-decisions/DECISION_QUEUE.md`. Once resolved: route redirect, tRPC gating, and schema constraint changes (FR-2.1–2.4), in that dependency order (schema decision must be settled before the other two layers can be implemented against it).

## V1.3 — Remediation scope (Backlog items, all GO — see `99-final/GO_NO_GO.md` for individual scoring)
- REN-152: consolidate checkout implementations (sequence after V1.1 so the unified module inherits the corrected logic).
- REN-153: cart availability visibility.
- REN-161: TRYNEW20 disclosure (after the short FR-5.1 investigation).
- REN-163: context-aware cancellation redirect.
- REN-108, REN-109, REN-110, REN-112: Guest Journey QA findings.

## Explicit triggers into V2 (none currently met)
No V1 item requires new capability beyond fixing what exists — see V2.md for what would need to be true to justify anything beyond V1.
