# V2 — P05 Customer Journey & UX

No V2 scope is currently justified by evidence gathered in this pass — this file exists per template and defines triggers rather than committing scope.

## Candidate V2 items, gated by explicit triggers

- **Order-then-pay architecture (BRule-1 alternative A, `07-feasibility/ALTERNATIVES.md`)**: revisit only if V1.1's transaction/reconciliation hardening proves insufficient in production (i.e., partial-order incidents continue at a non-trivial rate even after the transaction boundary ships) — trigger: post-V1.1 incident rate above an agreed threshold, to be set when V1.1 ships.
- **Webhook-as-source-of-truth redesign (Alternative B)**: revisit if FR-1.5's investigation into the current webhook handlers finds they are unreliable or duplicative with the client path in a way a simple ownership decision can't resolve — trigger: FR-1.5 investigation finding structural conflict, not just an ownership clarification.
- **Express/lightweight guest identity (email+OTP) as a REN-95 alternative to full anonymous guest orders**: revisit if the 6 blocking decisions land on "some identity capture required" rather than either extreme — trigger: decision-queue resolution, not an engineering-initiated choice.

## Not a V2 candidate
AI/personalization features remain NOT APPLICABLE at V2 as much as V1 — no evidence gathered in this Epic points to a prediction/ranking/generation opportunity in the purchase journey (see `07-feasibility/FEASIBILITY_ASSESSMENT.md`).
