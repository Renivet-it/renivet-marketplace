# V2 — P06

## Scope (conditional)
- **REN-166 (GA4 e-commerce events)** — ONLY if DECISION-P06-001 resolves in favor of keeping GA4 as a revenue-reporting source. If it does, V2 work is: instrument GA4 e-commerce events (`add_to_cart`, `begin_checkout`, `purchase`) mirroring the (by-then-fixed) PostHog/Meta event set.
- **REN-164 close-out** — if the verification (run alongside/after V1) finds a real init-timing capture-loss defect, the actual fix lands here.
- Consider building the shared `emitCommerceEvent()` abstraction (rejected for V1 in `07-feasibility/BUILD_REUSE_BUY_SIMPLIFY.md`) if GA4 is added — at that point the fan-out grows to four systems and the duplication cost of hand-writing each call site rises enough to justify it.

## Trigger condition
See `VERSION_TRIGGERS.md`. V2 does not start until DECISION-P06-001 is resolved (for REN-166) or REN-164's verification completes with a positive finding (for that item specifically).
