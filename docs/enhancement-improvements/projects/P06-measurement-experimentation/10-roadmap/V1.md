# V1 — P06

## Scope
- **REN-145** (P0, Urgent) — currency-unit fix + per-brand fan-out fix for the Meta Purchase event. Highest priority in this Epic; corrupts live ad-spend decisions today.
- **REN-131** — add server-side `purchase_completed` capture.
- **REN-132** — document `add_to_cart`/`cart_added` trigger semantics (no code merge).
- **REN-133** — consolidate duplicate `purchase_completed` instrumentation (design together with REN-131 per `07-feasibility/DEPENDENCIES.md`).
- **REN-134** — rename `src/lib/posthog/client.tsx` (naming/hygiene, Low priority, can trail the others).

## Rationale
These five items are all CONFIRMED, source-verified defects/gaps in the existing, already-shipped instrumentation. None require a product decision, new vendor, or new infrastructure. REN-145 in particular is P0/Urgent and should not wait on the others.

## Explicitly NOT V1
- REN-166 (GA4) — gated on DECISION-P06-001.
- REN-164 (init-timing verification) — should happen, but is a verification task that can run in parallel with or slightly ahead of V1 without blocking it; included here as a fast-follow rather than a hard V1 dependency.
