# V3 — P06

No concrete V3 scope is defensible from current evidence. Candidate, speculative items only, none committed:
- A formal, owned attribution model (currently delegated entirely to Meta/PostHog platform defaults) — only worth considering if a specific, evidenced gap in platform-provided attribution emerges; none is evidenced today.
- Automated anomaly detection on Meta-bound conversion values (flagged as a nice-to-have in `08-reliability/OBSERVABILITY.md`) to catch a future REN-145-class defect before it silently corrupts data for months.
- Cross-referencing `add_to_cart`/`cart_added` into a single combined funnel-view dashboard (deferred from V1's "keep them separate" decision in `05-algorithms/DECISION_LOGIC.md`) if analysts continue to find the two-event model confusing in practice.

None of these are scoped, sized, or justified by current evidence — listed only to avoid silently dropping ideas surfaced during this pass. Do not schedule any of them without new evidence.
