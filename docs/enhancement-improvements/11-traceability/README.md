# Traceability

The main traceability document lives at `../AUDIT_TO_BACKLOG_TRACEABILITY.md` (top level, per the program's own file-path specification). This folder exists to satisfy the program's required folder structure; it intentionally holds only this pointer rather than a duplicate copy.
