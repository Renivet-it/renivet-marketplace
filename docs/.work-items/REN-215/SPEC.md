# REN-215 — Festive SEO and performance readiness

Keep the existing festive canonical, sitemap inclusion, SSR, and `force-dynamic` behavior. Make campaign metadata/H1 current and configurable, add truthful featured-product ItemList/Product JSON-LD, use a festive OG asset, and set priority only on the genuinely above-fold image.

Validation requires rendered view-source, mobile/desktop visual inspection, schema validation, and an explicit note that revalidation is deferred.
