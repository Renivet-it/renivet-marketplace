schema_version: "1.0"
task:
    id: REN-141
    title: Remove Unused Puppeteer
    source: linear
    branch: ayanganguly333/ren-140-141-reliability-cleanup
    status: READY_FOR_DEV
    linear:
        url: https://linear.app/renivet/issue/REN-141/remove-unused-puppeteer
        priority: Low
        labels: []
        status: Backlog
        assignee: Ayan Ganguly
        project: null
        milestone: null
risk:
    initial_risk: L1
    path_rule_risk: L1
    semantic_risk: L1
    final_risk: L1
    reasons:
        - The change removes an unused direct production dependency and its browser-download transitive graph.
        - No application import or runtime path was found, but dynamic usage remains a residual risk requiring build verification.
        - The change is reversible through dependency metadata and lockfile restoration.
investigation:
    depth: dependency_tracing
    investigated_areas:
        - Linear issue, relations, status, branch context, and comments
        - package.json dependency declaration and both repository lockfiles
        - Repository-wide source search for Puppeteer and browser automation usage
        - Existing test and build scripts
    excluded_areas:
        - Application route, API, database, authentication, and provider logic
        - Production secrets and external service configuration
        - Unrelated dependency upgrades
    dependencies: [DEP-001]
    integrations: []
    security_boundaries: []
    state_transitions:
        - puppeteer_declared -> dependency_removed -> lockfile_resolved_without_direct_puppeteer
    related_tests: [TEXP-001, TEXP-002, TEXP-003]
    uncertainties:
        - A runtime dynamic import not visible to repository-wide static search must be ruled out by build and smoke verification.
requirements:
    - id: REQ-001
      description: Remove the unused puppeteer dependency from the package manifest and regenerate the authoritative lockfile(s).
      type: explicit
    - id: REQ-002
      description: Preserve build, test, and application behavior after the dependency graph changes.
      type: explicit
    - id: REQ-003
      description: Do not introduce unrelated dependency, source, configuration, or runtime changes.
      type: inferred
scenarios:
    - id: SCN-001
      requirement_ids: [REQ-001]
      description: The manifest and resolved lockfile graph no longer include Puppeteer solely as this direct dependency.
    - id: SCN-002
      requirement_ids: [REQ-002]
      description: Bun tests and the production build complete successfully without Puppeteer.
    - id: SCN-003
      requirement_ids: [REQ-003]
      description: Diff review and source search confirm the cleanup is dependency-scoped and no hidden usage is introduced or broken.
invariants:
    - id: INV-001
      description: No application runtime import may depend on Puppeteer after the cleanup.
    - id: INV-002
      description: Removing Puppeteer must not alter application routes, data, credentials, or external provider behavior.
flows:
    - id: FLOW-001
      description: Remove the manifest entry, regenerate lockfile metadata, resolve dependencies, and run build/test verification.
      state_transitions: [puppeteer_declared, dependency_removed, lockfile_resolved_without_direct_puppeteer, verified]
dependencies:
    - id: DEP-001
      name: Puppeteer package graph
      purpose: Current direct production dependency and bundled browser download.
      owner: Package manager and repository maintainers
      required_state: Absent unless a verified source/runtime consumer is discovered.
      description: Static repository search found no consumer; package.json and lockfile currently retain it.
      status: resolved
integrations: []
personas:
    - id: PER-001
      role: release_engineer
      applicability: Needs reproducible installs and successful production builds.
security_boundaries: []
business_rules: []
decisions:
    - id: DEC-001
      question: Which lockfile is authoritative for CI and Vercel after removing Puppeteer?
      class: AUTO_DECIDE
      status: resolved
      recommendation: Follow the repository’s existing Bun/CI policy and keep any tracked lockfile that the build system consumes consistent with package.json.
      basis: Bun is mandated for repository scripts/tests, while both bun.lock and package-lock.json are tracked; verification must inspect the resulting dependency graph.
      confidence: medium
      consequence: low
test_expectations:
    - id: TEXP-001
      category: regression
      classification: REQUIRED
      reason: Confirms the direct dependency and browser package graph are removed as intended.
      scenarios: [SCN-001]
    - id: TEXP-002
      category: integration
      classification: REQUIRED
      reason: Confirms the application still builds and tests without the package.
      scenarios: [SCN-002]
    - id: TEXP-003
      category: exploratory
      classification: REQUIRED
      reason: Final repository-wide usage search and diff inspection mitigate undiscovered dynamic usage and scope drift.
      scenarios: [SCN-003]
traceability:
    requirement_to_scenarios:
        - requirement_id: REQ-001
          scenario_ids: [SCN-001]
        - requirement_id: REQ-002
          scenario_ids: [SCN-002]
        - requirement_id: REQ-003
          scenario_ids: [SCN-003]
    scenario_to_invariants:
        - scenario_id: SCN-001
          invariant_ids: [INV-001]
        - scenario_id: SCN-002
          invariant_ids: [INV-002]
        - scenario_id: SCN-003
          invariant_ids: [INV-001, INV-002]
    scenario_to_test_expectations:
        - scenario_id: SCN-001
          test_expectation_ids: [TEXP-001]
        - scenario_id: SCN-002
          test_expectation_ids: [TEXP-002]
        - scenario_id: SCN-003
          test_expectation_ids: [TEXP-003]
critic:
    required: false
    completed: false
    artifact: null
    reviewer: null
    fresh_context: false
    read_only: true
    required_categories: []
    findings: []
approval:
    state: APPROVED
    design_blockers: []
    approved_by: Ayan Ganguly (owner confirmation)
implementation_review:
    artifact: REVIEW.md
    result: REVIEW_PASSED_WITH_FINDINGS
    base_branch: ayanganguly333/ren-169-replace-url-parse-react-pdf-image
    base_commit: "a68132340376b8841d26b88e2991f7c7f851014b"
    head_commit: "a68132340376b8841d26b88e2991f7c7f851014b"
    pr_url: null
    material_drift: NO_DRIFT
    reconciliation:
        requirements: PARTIAL
        scenarios: PARTIAL
        invariants: PASS
        architecture: PASS
        security: NOT_APPLICABLE
        test_coverage: PARTIAL
        scope: PASS
    blocking_findings: []
    required_actions:
        - "REV-001: Rerun bun run build in a clean, unoccupied checkout or CI environment before merging."
    evidence:
        - "Compared the approved REN-141 contract with the uncommitted dependency and lockfile diff from HEAD a68132340376b8841d26b88e2991f7c7f851014b."
        - "bun test completed with 116 passing tests, 1 skipped test, and 0 failures."
        - "bun run build timed out after five minutes after the initial .next trace permission issue was cleared."
    governance_reentry_required: false
