# REN-216 — Fix `/shop` metadata and page semantics

Update only the shop route metadata and page heading. Preserve Events metadata, query behavior, filters, and canonical behavior. The title must be descriptive and under 60 characters, the description unique and 150–160 characters, and `/shop` must render exactly one H1.

Validation: source-level and rendered view-source checks for `/shop` and `/events`.
